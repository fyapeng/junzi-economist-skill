# Round 10 — runtime discovery and automatic dispatch

## Claude Code environment

No `claude` command, native Windows binary, legacy local binary, or global npm package was present. Current official Claude Code documentation confirms that personal skills live under `~/.claude/skills/<name>/SKILL.md`, may be invoked as `/<name>`, and may be selected automatically from their description.

The transactional installer copied the complete package to `C:/Users/ENAN/.claude/skills/junzi-economist`; parity passed for the then-current 102 files. This proves installation layout, not runtime behavior. Installing and authenticating a third-party CLI was not inferred from the skill-development request, so runtime status remains blocked by a missing external dependency rather than being promoted from static evidence.

## Fresh Codex automatic-dispatch runs

- `JE-X016` positive research task: `pass`. Junzi Economist loaded automatically with only micro, methods, and welfare references.
- `JE-X017` pure translation: `pass`. No skill loaded.
- `JE-X018` live-claim abstract: `fail`. Only `econ-paper` loaded, so upstream claim governance did not run even though the prose happened to remain honest.

The skill description was revised to require Junzi Economist before downstream economics reading or writing skills whenever interpretation or prose could alter a live claim.

- `JE-X019` retest: behavior passed but was marked `invalid` because the description revision was not yet committed.
- Commit `fdb16e6` preserved the failed routing record and the description fix.
- `JE-X020` fresh retest on `fdb16e6`: `pass`. Load order was `junzi-economist` then `econ-paper`, and the abstract preserved the subgroup as exploratory and the mechanism as unresolved.

## Mainline decision

Codex automatic positive, negative, and coexistence routing now has strict behavioral evidence. Claude Code remains installed-layout compatible but runtime-untested until the user installs and authenticates the official CLI. The next in-scope work is legacy PDF/long-manuscript parity while awaiting that external dependency.

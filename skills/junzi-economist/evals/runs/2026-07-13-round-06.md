# Round 06 — constrained theory and mutable-source retests

## Scope

Blindly retest the dedicated theory-modeling and frontier-search protocols added after earlier failures. Preserve raw outputs and failed claims instead of replacing them with summaries.

## JE-X007: provider model

The response correctly projected the provider optimum onto `[0,T]`, derived the constrained social optimum, separated interior and corner payment effects, and supplied valid examples. Independent audit found a new benchmark-distance error: when treatment starts exactly at the social optimum, a nonzero change moves away even though the usual sign product equals zero. It also did not derive the payment condition for movement toward welfare, only for movement in treatment.

Verdict: `mixed`.

Action: add a distance-to-benchmark protocol to `THEORY_MODELING.md` and extend the deterministic fixture with departure-from-optimum counterexamples.

## JE-X008: current frontier

The response produced a useful mechanism map, competing explanations, null evidence, three discriminating designs, a query log, and a source matrix. It also recognized that the NBER landing page and current PDF for *Rationing by Race* exposed different revision dates. A direct download during adjudication returned the April 2026 PDF and was hashed, correcting one stale-cache conclusion in the independent audit.

Remaining failures were real: it conflated the Chinese notice's document and webpage dates, treated an author-page date as a manuscript revision, and claimed every mapped mechanism had evidence when some were retained hypotheses.

Verdict: `mixed`.

Action: separate document, webpage, revision, and retrieval dates; hash mutable artifacts; restrict stopping claims to mechanisms decisive for the current decision.

## Mainline decision

Do not declare either protocol proved. The revisions changed behavior in the intended direction, but each retest exposed a deeper failure. Preserve both mixed executions, improve the protocol at the newly identified decision point, and retest later without leaking these diagnoses.

# Round 12 — clean theory and frontier endpoints

## Purpose

Close the two remaining strict Codex endpoint gaps without reusing the earlier physician-model or frontier answers: exact constrained theory with executed kink checks, and current-source investigation that is willing to stop before an unsupported causal design.

## Theory endpoint

`JE-X025` used a new capacity-constrained abatement model. It derived the global private and social choices and a necessary-and-sufficient classification for every finite subsidy increase, including initial equality, symmetric crossing, overshooting, saturation, continuity, and all one-sided derivatives. The deterministic script passed 100 parameter triples, 5,096 objective checks, 31,200 ordered subsidy pairs, and 600 fiscal-resource-cost cases.

The first independent audit was `mixed` despite correct mathematics: the fiscal extension was not yet executed, and the phrase `verified-global` blurred analytic proof with finite-grid evidence. The branch returned to computation and evidence language, added exact fiscal checks, and distinguished strict-concavity and algebraic proofs from implementation regressions. The preserved retest passed at 98/100. This corrective history remains in `audit.md` rather than being erased.

## Frontier endpoint

`JE-X026` freshly verified the current NBER manuscript, China NHC policy text, and NHS England referral-diversion page. It separated issue, document, effective, webpage, revision, server, and retrieval dates; preserved the NBER cache/current-artifact conflict; hashed the current 69-page April 2026 PDF; and disclosed the NHC page's HTTP 412 byte-retrieval limit.

The endpoint rejected the inference that a policy, early adopter, target year, capacity label, or national-rollout statement supplies causal assignment. It restored the complete recommendation–observed information–patient choice–capacity–acceptance–allocation–outcome chain, paused at the Shi/Shu boundary, and stated five operational reopening routes. Independent source replication passed. A minor dynamic-HTML reproducibility note was corrected by labeling hashes as timestamped ordinary-GET response snapshots and treating the PDF hash as the manuscript anchor.

## Decision

Mark `theory_modeling` and `institutions_and_frontier` proved for the release-candidate audit. Their remaining gaps concern later breadth, not the clean endpoint gate. The unresolved release blockers are now external: real Claude Code runtime behavior and a public remote with anonymous installation testing.
